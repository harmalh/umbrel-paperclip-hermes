The pinned upstream Paperclip source already includes `hermes-paperclip-adapter`
and registers `hermes_local` in `server/src/adapters/registry.ts`.

This directory is kept for future compatibility patches if upstream removes or
changes that registration path in a later version.
